DP - 메모이제이션 (memoization => memo)
동일한 계산을 반복하는 것을 피하기 위해 계산 결과를 저장해두고 사용하는 것
하향식으로 이미 계산했는지 여부를 확인하고 이미 계산한 값이 있으면 다시 계산하지 않고 활용함
상향식으로 하는 방식은 타뷸레이션(Tabulation)이라고 함

예) 피보나치 수열을 구하는 함수를 fi(n) 이라고 하면
fi(n) = fi(n-1) + fi(n-2) 방식으로 단순 계산하면
fi(5) 를 계산하려면 fi(4) + fi(3) = fi(3) + fi(2) + fi(2) + fi(1) = fi(2) + fi(1) + fi(1) + fi(0) .... 이런식으로 중복 계산을 해줘야함

dp 라는 배열에 계산한 값을 저장한다면
fi(n)을 계산할 때 dp에 fi(n) 계산결과가 있는지 확인하고 있으면 그대로 사용, 없으면 구해준 후 저장하는 방식

ㅁㅁ

